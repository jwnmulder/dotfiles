{{- $tar_args := (list "--extract" "--file" "/dev/stdin" "--gzip" "--to-stdout") -}}
type: file
  executable: true
  filter:
    command: tar
    args: [{{ (concat $tar_args .) | quoteList | join ", " }}]
